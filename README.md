# kryptonim application

# To run:
- [ ] Update kryptonim/config/dev.json "API_KEY" field with your key
- [ ] Run command `docker build -t kryptonim-app .`
- [ ] Run command `docker run -p 8081:8081 kryptonim-app`
